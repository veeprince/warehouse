class Product {
  final String name, quantity, productLocation, productPosition, imageUrl;

  Product(
      {required this.name,
      required this.quantity,
      required this.productLocation,
      required this.productPosition,
      required this.imageUrl});
}
